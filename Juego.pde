class Juego {

  // -----------------------------------------------------
  // PROPIEDADES (variables)
  // EstadoGeneralDelJuego = menu, jugar, ganar, perder ---------¿Debemos tener menú?
  // Arreglo de Jugador
  // Arreglo de Obstaculos
  // Arreglo de Enemigos
  // Escenario (¿Se llama al escenario dentro de las propiedades de la clase Juego?)

  // -----------------------------------------------------
  // CONSTRUCTOR (setup del objeto)
  // EstadoGeneralDelJuego = "menu";
  // inicializar los objetos: jugador, enemigos y obstáculos
  //


  // -----------------------------------------------------
  // METODOS (funciones) 
  // actualizar()
  // --jugador.actualizar();
  // --enemigos.actualizar();
  // --obstaculos.actualizar();
  // --escenario.actualizar();
  
  // dibujar()
  // --logicaDeEstados
  // --llamar a los metodos dibujar de mis objetos
  // --jugador.dibujar();
  // --enemigos.dibujar();
  // --obstaculos.dibujar();
  // --escenario.dibujar();
  
  // reciclar()
  // --jugador.reciclar();
  // --enemigos.reciclar();
  // --obstaculos.reciclar();
  // --escenario.reciclar();
}
