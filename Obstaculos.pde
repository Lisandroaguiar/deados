class Obstaculos {
  // -----------------------------------------------------
  // PROPIEDADES > variables
  // variables para posición, velocidad y tamanio
  // variables para la imagen
  //PImage [] (arreglos de imagenes)
  // -----------------------------------------------------
  // CONSTRUCTOR > el "setup de la clase"
  // inicializar las propiedades/variables
  // IMAGENES
  // carga de las imagenes
  /*for (int i = 0; i < numFrames; i++) {
      lata[i] = loadImage("NOMBREARCHIVO”+ i + ".png");
    }
for (int i = 0; i < numFrames; i++) {
     botella[i] = loadImage("NOMBREARCHIVO”+ i + ".png");
  */
  // -----------------------------------------------------
  // METODOS > funciones
  // void actualizarObstaculos()
  // -- mueve los obstaculos

  // void dibujarObstaculos()
  // --dibuja la imagen en la posicion x/y de la clase y recorre el arreglo

  // void reciclarObstaculos()
  // --recicla todas las propiedades al valor inicial de los obstáculos

}
