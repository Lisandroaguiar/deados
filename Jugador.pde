class Jugador {

  // -----------------------------------------------------
  // CAMPOS (variables) #propiedades
  // Posición, tamaño y velocidad
  // variables para la imagen
  //PImage [] (arreglos de imagenes)
  // -----------------------------------------------------
  // CONSTRUCTOR > metodo/funcion inicial (el "setup" del objeto)
  // inicializar posicion y tamaño mediante parámetros (propiedades/variables)
  // IMAGENES
  // carga de las imagenes
  /*for (int i = 0; i < numFrames; i++) {
   jugador[i] = loadImage("NOMBREARCHIVO”+ i + ".png");
   }
   */
  // -----------------------------------------------------
  // METODOS (funciones) #acciones
  // dibujar()
  // --mostrar los sprites de mi personaje 

  // METODOS para KeyPressed()
  // moverIzq()
  // --mueve al personaje hacia la izquierda con la flecha correspondiente
  //
  // moverDer()
  // --mueve al personaje hacia la derecha con la flecha correspondiente
}
